This is a visualizer for the so100 arm. It uses python 3.13.5
Run "uv pip install -r requirements.txt" then "python visualize_arm.py".
The visualizer will tell you what port the localhost is on.

You can change where the arm will try to go by moving the sliders on the right side of the visualizer.
